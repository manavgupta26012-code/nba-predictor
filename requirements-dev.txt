# Extra dependencies for running the src/collect_*.py scripts and the
# automated GitHub Actions workflows. NOT needed by the deployed app
# itself (see requirements.txt).

nba_api>=1.4
requests>=2.32
pdfplumber>=0.11
PyYAML>=6.0
